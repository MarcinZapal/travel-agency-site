Git:
* git status
* git stage -A
* git commit -m 'comment'
* git push origin master
NPM - for paackages
* npm init -y
* npm install package
* npm install css-loader style-loader --save-dev
* npm install webpack-dev-server // for css & js updaged auto in browser, reload the browser, view site on different devices connected to save wifi
* npm run dev
webpack - to bandle js into one
* install webpack from npm
* add to package.json script dev to run webpack
* create webpack.config.file
* npm run dev for manual run

// -------
BEM:
* Block - independent reusable part of design
* Element - belong to the block, it can not be ued outside of the Block
* Modifier - can be used on a block or an elemnt to indicae a change to the default state of an object

//--------
font-size: 1.rem rem = relative to the root of page <html>